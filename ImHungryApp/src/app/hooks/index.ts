/**
 * App Hooks Module
 * 
 * Exports application-level hooks for bootstrap and lifecycle management.
 */

export { useBootstrap, default } from './useBootstrap';
export type { BootstrapState, UseBootstrapOptions } from './useBootstrap';
